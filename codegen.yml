overwrite: true
schema: "schema/**/*.graphql"
documents: null
generates:
  src/generated/graphql.ts:
    config:
      scalars:
        Date: Date
    plugins:
      - "typescript"
      - "typescript-resolvers"
